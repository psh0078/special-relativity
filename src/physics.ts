export function compute_lorentz_gamma(beta: number): number {
  return 1 / Math.sqrt(1 - beta^2)
}

// export function compute_velocity_addition(isLabFrame: boolean, vinput: number, vcurrentframewrtlab: number): number {
//   if (isLabFrame) {

//   }
// }


